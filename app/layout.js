export default function RootLayout({ children }) {
  return (
    <div className="max-w-5xl mx-auto">{children}</div>;
    <html>
      <body>{children}</body>
    </html>
  );
}

<div className="max-w-5xl mx-auto">{children}</div>
